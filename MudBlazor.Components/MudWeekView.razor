@using MudBlazor.Rendering
@inherits SchedulerViewBase

@code {
    [Parameter] public IReadOnlyCollection<Appointment> Appointments { get; set;  }

    public override string Header => "Week view";

    public override  RenderFragment Render()
    {
        return @<WeekView Appointments="Scheduler.Appointments" />;
    }
}
